import React, { useState } from 'react'; const QuizView = ({ quiz, onComplete }) => { const [answers, setAnswers] = useState({}); const [score, setScore] = useState(null); if (!quiz || !quiz.questions) { return <div style={{ padding: '2rem', color: 'white' }}>No Quiz Found</div>; } const handleOptionSelect = (questionIndex, option) => { setAnswers(prev => ({ ...prev, [questionIndex]: option })); }; const handleSubmit = () => { let correct = 0; quiz.questions.forEach((q, index) => { if (answers[index] === q.answer) { correct++; } }); const finalScore = Math.round( (correct / quiz.questions.length) * 100 ); setScore(finalScore); }; if (score !== null) { return ( <div style={{ color: 'white', textAlign: 'center' }}> <h2>Quiz Completed</h2> <h3>Your Score: {score}%</h3> <button onClick={onComplete} className="btn btn-primary" > Return </button> </div> ); } return ( <div style={{ color: 'white' }}> <h2 style={{ marginBottom: '2rem' }}> {quiz.courseTitle} Quiz </h2> {quiz.questions.map((q, qIndex) => ( <div key={qIndex} style={{ marginBottom: '2rem', padding: '1rem', border: '1px solid #333', borderRadius: '10px' }} > <h3> {qIndex + 1}. {q.question} </h3> {q.options.map((option, oIndex) => ( <div key={oIndex}> <label> <input type="radio" name={`question-${qIndex}`} value={option} checked={answers[qIndex] === option} onChange={() => handleOptionSelect(qIndex, option) } /> {' '} {option} </label> </div> ))} </div> ))} <button onClick={handleSubmit} className="btn btn-primary" > Submit Quiz </button> </div> ); }; export default QuizView;